export class App {
    port;
    hostname;
    constructor(props = {}) {
        this.port = props.port ?? 3000;
        this.hostname = props.hostname ?? "0.0.0.0";
    }
}
//# sourceMappingURL=App.js.map